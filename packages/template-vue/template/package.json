{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vue build --config config.js",
    "build": "vue build --config config.js --prod"
  },
  "author": {
    "name": "{{ username }}",
    "email": "{{ email }}"
  },
  "devDependencies": {
    "vue-cli": "egoist-robot/vue-cli#vue-build",
    "postcss-nested": "^1.0.0"
  }
}
